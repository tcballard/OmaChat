# Parser fuzzing

`hostile-codecs` feeds the externally reachable bounded decoders with a shared
arbitrary byte stream. The scheduled workflow runs it for five minutes with a
1 GiB RSS ceiling. Reproduce a finding with the artifact generated by
`cargo fuzz`; never commit a corpus containing private traffic.

```sh
cargo install cargo-fuzz --locked
cargo fuzz run hostile-codecs
```
